"""Shared defaults reused across fake GCP resource config files."""

PROJECT_ID = "acme-analytics-prod"
REGION = "us-central1"

COMMON_LABELS = {
    "owner": "platform-engineering",
    "cost_center": "analytics",
    "managed_by": "assets-demo",
}
